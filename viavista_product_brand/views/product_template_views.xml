<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="product_template_form_brand" model="ir.ui.view">
        <field name="name">product.template.form.brand</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='categ_id']" position="after">
                <field name="brand_id"/>
            </xpath>
        </field>
    </record>

    <record id="product_template_list_brand" model="ir.ui.view">
        <field name="name">product.template.list.brand</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_tree_view"/>
        <field name="arch" type="xml">
            <field name="categ_id" position="after">
                <field name="brand_id" optional="show"/>
            </field>
        </field>
    </record>

    <record id="product_template_search_brand" model="ir.ui.view">
        <field name="name">product.template.search.brand</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_search_view"/>
        <field name="arch" type="xml">
            <field name="categ_id" position="after">
                <field name="brand_id"/>
            </field>
            <filter name="group_by_categ_id" position="after">
                <filter string="Brand" name="group_by_brand" context="{'group_by': 'brand_id'}"/>
            </filter>
        </field>
    </record>

</odoo>
